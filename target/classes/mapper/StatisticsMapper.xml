<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wl.mapper.StatisticsMapper">
    <select id="getUserCount" resultType="int">
        SELECT COUNT(*) FROM user;
    </select>

    <select id="getBookCount" resultType="int">
        SELECT COUNT(*) FROM book;
    </select>

    <select id="getCategoryCount" resultType="int">
        SELECT COUNT(*) FROM book_category;
    </select>

    <select id="getActivityCount" resultType="int">
        SELECT COUNT(*) FROM activity;
    </select>

    <select id="getCurrentlyBorrowedCount" resultType="int">
        SELECT COUNT(*) FROM borrowingbooks;
    </select>
    <select id="getFeedbackCount" resultType="int">
        SELECT COUNT(*) FROM feedback;
    </select>
</mapper>